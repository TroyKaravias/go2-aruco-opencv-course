# G1 Scripts

Place G1 humanoid-specific scripts in this folder.

Examples:
- `g1_aruco_scan.py`
- `g1_aruco_autonomous.py`

Keep shared utilities (marker generation, generic helpers) in the parent `scripts/` folder.